#include<iostream>

using namespace std;

int main() {
	int n;
	cin >> n;
	for (int i = 1; 1; i++) {
		if (n * i <= 20) {
			cout << n<< "*" << i << "=" << n * i;
			cout << endl;
		}
		else
			break;
		
	}
		
	return 0;
}
