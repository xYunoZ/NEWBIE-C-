#include<iostream>
#include<math.h>

using namespace std;

int main() {
	int n;
	cin >> n;
	int sum = 0;
	int i = 1;
	int a;
	do {
		cin >> a;
		sum += a;
		i += 1;
	} while (i <= n);
	float av = sum / n;
	cout << sum << " " << av;

	return 0;
}
